1.	Один и тот же набор полей для Bot / Individual / Org / DAO — это слишком плоско.
2.	Category как free-text — это гарантированный хаос через 3 недели.

Давай разберём это системно.

⸻

1️⃣ Разные типы → разная семантика, а не просто label

Сейчас у тебя agent.type — это почти декоративное поле.
Но если тип выбран, UI должен адаптироваться, иначе это не тип, а тег.

Я бы сделал модель:

Общие поля (для всех)

Эти остаются всегда:
	•	Display Name
	•	Description
	•	Category (через controlled tags)
	•	Payment Wallet
	•	MCP Endpoint (опционально, если не нужен — скрывается)
	•	Avatar / Image

⸻

🔹 BOT (Automated service)

Фокус: capability и интерфейсы.

Поля:
	•	MCP Endpoint (обязательное)
	•	Supported Capabilities (multi-select)
	•	Rate Limit (optional)
	•	GitHub URL
	•	Docs URL
	•	API Schema URL

Логика:
Если выбран BOT → UI усиливает routing + tech-метаданные.

⸻

🔹 INDIVIDUAL

Фокус: персональная идентичность.

Поля:
	•	Display Name
	•	Short Bio
	•	Website
	•	Socials (telegram / twitter / github)
	•	Optional MCP Endpoint (если это personal agent)
	•	Proofs (manual input)

Никакого “API schema” тут не нужно.

⸻

🔹 ORGANIZATION

Фокус: бренд + доверие.

Поля:
	•	Legal Name
	•	Jurisdiction (optional)
	•	Website
	•	GitHub Org
	•	Contact Email
	•	Team Size (optional)
	•	MCP Endpoint
	•	Payment Wallet (можно multi-sig indicator)

⸻

🔹 DAO

Фокус: governance.

Поля:
	•	Governance Contract
	•	Snapshot URL
	•	Treasury Address
	•	Forum URL
	•	MCP Endpoint (если есть агент-обвязка)

⸻

Как это реализовать технически

UI:

const baseFields = [...]
const typeSpecificFields = {
  BOT: [...],
  INDIVIDUAL: [...],
  ORG: [...],
  DAO: [...]
}

При смене типа:
	•	сохраняешь общие поля
	•	type-specific блок перерисовывается
	•	значения, не относящиеся к новому типу, либо очищаются, либо сохраняются в draft (чтобы не потерять данные)

Это не только UX — это структурирование identity-данных на уровне протокола.

⸻

2️⃣ Category — free text нельзя

Free text =
“ai agent”,
“AI-agent”,
“AI”,
“machine learning thing”,
“web3 infra”

Через месяц это будет нечитаемый ад.

Тебе нужно:

Controlled vocabulary + searchable multi-select

UI-паттерн:
	•	вводишь текст
	•	появляется dropdown
	•	выбираешь
	•	превращается в badge/tag
	•	можно добавить несколько

Да, это tags, но не свободные.

⸻

3️⃣ Как построить Category систему правильно

Я бы сделал двухуровневую модель:

Level 1: Domain
	•	AI
	•	Infrastructure
	•	Finance
	•	Governance
	•	Commerce
	•	Security
	•	Data
	•	Media
	•	Social
	•	DevTools

Level 2: Subcategory

Например:

AI:
	•	LLM
	•	Agent
	•	Training
	•	Evaluation
	•	Inference
	•	Prompt Engineering

Finance:
	•	Payments
	•	Analytics
	•	Risk
	•	AML
	•	Trading
	•	Lending

DevTools:
	•	SDK
	•	API
	•	Monitoring
	•	CI/CD
	•	Testing

UI:
	•	сначала выбираешь domain
	•	потом subcategory
	•	всё превращается в badges

Это даст:
	•	нормальную фильтрацию
	•	поиск
	•	агрегирование
	•	future ranking

⸻

4️⃣ Маленький, но важный UX момент

Не называй это “Category”.

Назови:
	•	“Focus Areas”
или
	•	“Capabilities”
или
	•	“Domains”

“Category” звучит как маркетплейс 2016 года.

⸻

5️⃣ Архитектурный момент

Если ты сделаешь dynamic fields по type — это сразу повлияет на JSON Schema.

Лучший подход:

В schema оставить:

agent.type
agent.profile
agent.extensions

А тип-специфичные поля класть в:

extensions.bot
extensions.individual
extensions.org
extensions.dao

Это:
	•	не ломает базовую схему
	•	позволяет расширять
	•	не заставляет менять контракт

⸻

6️⃣ Что я бы сделал прямо сейчас

Минимальный MVP без перегруза:

✔ Общие поля
✔ Type-specific секция (collapsible card)
✔ Controlled multi-select tags (searchable)
✔ Ограничить максимум 5–7 tags
